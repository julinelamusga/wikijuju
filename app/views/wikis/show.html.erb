<h1><%= @wiki.name %></h1>

<%= render partial: 'wikis/page', collection: @pages %>

<p>
  <%= link_to "Create a New Page", new_page_path(wiki_id: @wiki.id), class: 'btn btn-primary btn-medium' %>
</p>